/**
 * Type declarations for custom themes
 * Extends the smrt-svelte theme system with havesmrt.com custom themes
 */

declare module '@happyvertical/smrt-svelte/themes' {
	/**
	 * Extended theme presets including custom havesmrt.com themes
	 */
	export type ThemePreset = 'material' | 'glass' | 'studio' | 'swiss';
}

export {};
