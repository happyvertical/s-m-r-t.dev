# The current s-m-r-t and HappyVertical packages are public on npm. Keep this
# project-local override so a parent or user-level GitHub Packages setting does
# not redirect clean installs to the old registry.
@happyvertical:registry=https://registry.npmjs.org/
engine-strict=true
