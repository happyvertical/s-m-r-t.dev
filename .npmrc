# @happyvertical/* packages are published publicly to npmjs (migrated off
# GitHub Packages). Public reads need no auth token, so none is configured here.
engine-strict=true
@happyvertical:registry=https://registry.npmjs.org
